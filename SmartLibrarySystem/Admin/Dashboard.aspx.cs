using System;

public partial class Admin_Dashboard : System.Web.UI.Page
{
    protected void Page_Load(object sender, EventArgs e)
    {
        // In a real app, we would fetch the actual counts from the DB 
        // and inject them into the data-target attributes or via an AJAX API call.
        // For visual appeal and demo purposes, the JS handles the animation.
    }
}
